#include<stdio.h>

int main(int argc, char *argv[])
{
    int i = 0;
   
    //not David's way but trying to use function instead 
    if(!isdigit(argv[i])){
        printf("Number\n");
    }     
    return 0;    
}
